'use client';

import Applications from '@/features/Applications';

/** Same page; the create guide-library modal opens because the path matches. */
export default function CreateApplicationPage() {
  return <Applications />;
}
